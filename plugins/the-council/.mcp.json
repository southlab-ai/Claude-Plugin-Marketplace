{
  "mcpServers": {
    "the-council": {
      "command": "uv",
      "args": ["run", "--directory", "${CLAUDE_PLUGIN_ROOT}", "python", "-m", "src.server"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}"
    }
  }
}
